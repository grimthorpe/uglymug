cmake_minimum_required (VERSION 3.12.0)

project(uglymug)

add_subdirectory(libugly)
add_subdirectory(game)

# The tools need to be dragged into the mudstring era, so only 2 decades out of date.
#add_subdirectory(tools)
